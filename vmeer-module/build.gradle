apply plugin: 'com.android.library'
android {
    namespace 'com.vmeer.io'
    compileSdk 34
    defaultConfig {
        minSdk 24
        targetSdk 34
        ndk { abiFilters 'arm64-v8a', 'armeabi-v7a' }
    }
    externalNativeBuild {
        ndkBuild { path 'src/main/jni/Android.mk' }
    }
}
